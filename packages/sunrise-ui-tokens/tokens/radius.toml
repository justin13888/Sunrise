# Corner radii, in pixels, per docs/07-clients/shared-ui-system.md §Radius.
#
# `pill` is deliberately larger than any surface it can be applied to: a radius
# clamped to half the shorter side is how every platform draws a capsule.
sm = 4
md = 8
lg = 12
pill = 9999
