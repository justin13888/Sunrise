[package]
name = "sunrise-log"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
publish = false
description = "Redaction layer and subscriber bootstrap for tracing-based Sunrise logging"

[lints]
workspace = true

[dependencies]
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
parking_lot = { workspace = true }
thiserror = { workspace = true }
zeroize = { workspace = true }
# The sanctioned wall-clock read for log timestamps; see `src/time.rs` for why
# this crate is exempt from the injected-`Clock` rule.
jiff = { workspace = true }

[dev-dependencies]
proptest = { workspace = true }
serde_json = { workspace = true }
jsonschema = { workspace = true }
